{% extends '::base.html.twig' %}

{% block body -%}

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ entity.status }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ entity.enabled }}</td>
            </tr>
            <tr>
                <th>Viewstate</th>
                <td>{{ entity.viewState }}</td>
            </tr>
            <tr>
                <th>Accessmin</th>
                <td>{{ entity.accessMin }}</td>
            </tr>
            <tr>
                <th>Filepath</th>
                <td>{{ entity.filePath }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Categoryid</th>
                <td>{{ entity.categoryId }}</td>
            </tr>
            <tr>
                <th>Inheritglobal</th>
                <td>{{ entity.inheritGlobal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('secure_project_edit', { 'id': entity.id }) }}">
        Edit
    </a>
{{ form_start(delete_form) }}
{{ form_widget(delete_form) }}
{{ form_end(delete_form) }}
{% endblock %}
