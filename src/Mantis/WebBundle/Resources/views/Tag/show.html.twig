{% extends '::base.html.twig' %}

{% block body -%}

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ entity.userId }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Datecreated</th>
                <td>{{ entity.dateCreated }}</td>
            </tr>
            <tr>
                <th>Dateupdated</th>
                <td>{{ entity.dateUpdated }}</td>
            </tr>
        </tbody>
    </table>

            <a href="{{ path('secure_tag_edit', { 'id': entity.id }) }}">
        Edit
    </a>
{{ form_start(delete_form) }}
{{ form_widget(delete_form) }}
{{ form_end(delete_form) }}
{% endblock %}
